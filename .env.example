# Node environment (set to production for Netlify deployment)
NODE_ENV=production

# Web3Forms access key
# Get your access key from https://web3forms.com/
WEB3FORMS_ACCESS_KEY=your_web3forms_access_key_here

# Buzzsprout API configuration (VITE_ prefix required for client-side access)
VITE_BUZZSPROUT_API_TOKEN=your_buzzsprout_api_token_here
VITE_BUZZSPROUT_PODCAST_ID=2489424

# Optional: Database (not required for current deployment using Web3Forms)
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Optional: Server port (used in development only)
# PORT=5000